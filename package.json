{
  "name": "fabric-to-image-data",
  "version": "0.1.3",
  "description": "Extend toImageData function for fabric.js",
  "main": "dist/fabric-to-image-data.min.js",
  "engines": {
    "node": ">=6.0.0"
  },
  "scripts": {
    "lint": "./node_modules/.bin/eslint --config .eslintrc.json build.js lib",
    "build": "node build.js",
    "prepublishOnly": "npm run build",
    "pretest": "npm run lint",
    "test": "./node_modules/.bin/mocha"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/josheplibra/fabric-to-image-data"
  },
  "keywords": [
    "fabricjs",
    "image",
    "image-data",
    "bitmap"
  ],
  "author": "josheplibra",
  "license": "MIT",
  "dependencies": {
  },
  "optionalDependencies": {
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "contextify": "^1.0.0",
    "eslint": "5.7.0",
    "fabric": "^2.4.3",
    "mocha": "^5.2.0",
    "mock-require": "^3.0.2",
    "nanoid": "^1.3.1",
    "uglify-js": "3.4.9"
  }
}
